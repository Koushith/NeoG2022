/**
 * const with arrays
 */
// const array = [1, 2, 3, 4];
// array.push(55); // this returns the length after pushing

// console.log(array);

const array = [1, 2, 3, 4];
array = array.push(55); //assignment to constant variable
